[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: fast unit tests, no I/O or external dependencies
    integration: requires disk I/O and fixtures
    rtsp: requires a live RTSP camera (skip in CI)
asyncio_mode = auto
